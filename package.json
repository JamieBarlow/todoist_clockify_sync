{
  "dependencies": {
    "@doist/todoist-api-typescript": "^4.0.0-alpha.2",
    "date-fns": "^4.1.0",
    "dotenv": "^16.4.7",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "colors": "^1.4.0"
  },
  "scripts": {
    "build": "npx tsc",
    "start-todoist": "npm run build && node dist/todoist.js",
    "start-clockify": "npm run build && node dist/clockify.js",
    "start": "npm run build && node dist/main.js",
    "sync-meetings": "npm run build && node dist/syncMeetings.js",
    "close-tasks": "npm run build && node dist/closeTasks.js",
    "reschedule": "npm run build && node dist/rescheduleOverdue.js"
  }
}
